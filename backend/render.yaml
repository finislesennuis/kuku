services:
  - type: web
    name: sejong-festival-api
    runtime: python
    pythonVersion: 3.11.9
    buildCommand: 'pip install -r requirements.txt'
    startCommand: 'uvicorn main:app --host=0.0.0.0 --port=$PORT'
    envVars:
      - key: PORT
        value: 10000
